# npm 12 起 git 依赖默认全禁（allow-git=none），而 litepet-adapter-ts 是我们自己
# 写的包、没有发到 npm，只能从 git 源取。
#
# 这里只放开「本项目 package.json 里直接声明的那一层」：传递依赖里若还有 git 依赖，
# 照样会被拒。项目级配置随仓库一起走，clone 下来直接 npm install 即可，不必改全局。
allow-git=root
